breeze-test-gen {
  // during .breeze-file parsing, some temporary files could be created
  // specify, if a temporary directory should be created and deleted afterwards for this
  use-tmp-dir: true
}